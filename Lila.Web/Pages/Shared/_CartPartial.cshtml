@if (User.Claims.ToList().Find(claim => claim.Type == "Customer") != null)
{
    <a class="btn btn-outline-primary pe-4 ps-4" asp-page="/Order/DoOrder">
        <i class="fa fa-cart-arrow-down"></i>
    </a>
}
    